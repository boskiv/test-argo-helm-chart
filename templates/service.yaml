apiVersion: v1
kind: Service
metadata:
  name: {{ include "load-chart.fullname" . }}
  labels:
    {{- include "load-chart.labels" . | nindent 4 }}
  annotations:
    argocd.argoproj.io/sync-wave: "1"
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "load-chart.selectorLabels" . | nindent 4 }}
