apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "load-chart.fullname" . }}-test-connection"
  labels:
    {{- include "load-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "load-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
